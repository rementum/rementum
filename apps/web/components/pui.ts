"use client";

// performative-ui ships no "use client" directives of its own, so every RSC must import
// its components through this barrel instead of from the package directly.
export {
  AsciiHero,
  Aurora,
  BigBack,
  Button,
  CommunityBadge,
  EyebrowPill,
  FloatingSparkles,
  GlassCard,
  GradientText,
  LogoMarquee,
  MockIDE,
  NodeGraphBackground,
  Popover,
  Sparkle,
  StatCounter,
  StatusDot,
  StickyBanner,
  WibblingSpinner,
  WordRoll,
} from "performative-ui";
