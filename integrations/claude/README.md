# Claude and Claude Code

Remote connector URL: `https://YOUR_HOST/mcp`.

Claude Code:

```bash
claude mcp add --transport http owl-memory https://YOUR_HOST/mcp
```

Complete OAuth in the browser, then ask Claude to call `list_brains` and `get_brain`.
